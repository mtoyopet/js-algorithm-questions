// --- 問題
// 引数a、よりも引数bの値が大きければtrue、小さければfalseを返す関数sampleを完成させよ

// --- 例
// sample(2, 3) -> true
// sample(15, 2) -> false

// -- JESTを実行するコマンド
// jest sample_question/test.js

function sample(a, b) {
  // if (a < b) { return true }

  // return false
}

module.exports = sample;
